# No vars required for local dev — the Vite proxy forwards /api to localhost:3000
# Add VITE_API_URL for production deployments where the proxy isn't available
# VITE_API_URL=https://api.your-domain.com
